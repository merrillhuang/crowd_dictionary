<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to "Crowd Dictionary", root_path, class: "navbar-brand" %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to (I18n.t("navbar.translate")), language_search_path, class: "nav-link" %>
        </li>
      </ul>
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to (I18n.t("navbar.languages")), languages_path, class: "nav-link" %>
        </li>
      </ul>

      <ul class="navbar-nav ms-auto">
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <%= (I18n.t("navbar.site_language")) %>
            </a>
            <div class="dropdown-menu">
              <%= link_to "English", request.original_fullpath.sub("locale=cn", "locale=en"), class: "dropdown-item" %>
              <%= link_to "中文", request.original_fullpath.sub("locale=en", "locale=cn"), class: "dropdown-item" %>
            </div>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <%= current_user.email %>
            </a>
            <div class="dropdown-menu">
              <%= link_to (I18n.t("navbar.sign_out")), destroy_user_session_path, method: :delete, class: "dropdown-item" %>
            </div>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to (I18n.t("navbar.sign_in")), new_user_session_path, class: "nav-link" %>
          </li>

          <li class="nav-item">
            <%= link_to (I18n.t("navbar.sign_up")), new_user_registration_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
